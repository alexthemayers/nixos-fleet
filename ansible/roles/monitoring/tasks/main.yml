---
# =============================================================================
# Role: monitoring — tasks
# Installs lm-sensors and rasdaemon for hardware health monitoring.
# Deploys a CPU temperature check script run via cron every 5 minutes.
# =============================================================================

- name: Install hardware monitoring packages (lm-sensors, rasdaemon)
  ansible.builtin.apt:
    name:
      - lm-sensors
      - rasdaemon
    state: present
    update_cache: yes

- name: Deploy CPU temperature check script to /usr/local/bin/check-temps.sh
  ansible.builtin.template:
    src: check-temps.sh.j2
    dest: /usr/local/bin/check-temps.sh
    owner: root
    group: root
    mode: "0755"

- name: Deploy cron job to run CPU temperature check every 5 minutes
  ansible.builtin.copy:
    dest: /etc/cron.d/check-temps
    owner: root
    group: root
    mode: "0644"
    content: |
      # Managed by Ansible — do not edit manually. See ansible/roles/monitoring/
      # Run CPU temperature check every 5 minutes; logs to journald via logger.
      */5 * * * * root /usr/local/bin/check-temps.sh

- name: Enable and start rasdaemon (Machine Check Exception / hardware error logger)
  ansible.builtin.systemd:
    name: rasdaemon
    enabled: yes
    state: started

- name: Deploy hardware telemetry exporter script to /usr/local/bin/hardware-telemetry.py
  ansible.builtin.template:
    src: hardware-telemetry.py.j2
    dest: /usr/local/bin/hardware-telemetry.py
    owner: root
    group: root
    mode: "0755"

- name: Deploy hardware-telemetry systemd service
  ansible.builtin.copy:
    src: hardware-telemetry.service
    dest: /etc/systemd/system/hardware-telemetry.service
    owner: root
    group: root
    mode: "0644"
  notify: reload systemd

- name: Deploy hardware-telemetry systemd timer
  ansible.builtin.copy:
    src: hardware-telemetry.timer
    dest: /etc/systemd/system/hardware-telemetry.timer
    owner: root
    group: root
    mode: "0644"
  notify: reload systemd

- name: Flush handlers to ensure daemon-reload before starting hardware-telemetry timer
  ansible.builtin.meta: flush_handlers

- name: Enable and start hardware-telemetry timer
  ansible.builtin.systemd:
    name: hardware-telemetry.timer
    enabled: yes
    state: started

- name: Run initial hardware-telemetry collection
  ansible.builtin.systemd:
    name: hardware-telemetry.service
    state: started

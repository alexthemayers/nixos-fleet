.idea/

# Nix
result
result-*

# direnv
.direnv/

# Python
__pycache__/
*.py[cod]
*$py.class

# Editor / OS
.DS_Store
*~

# Recreated by attic login / local fills; never commit tokens
.attic-token
.attic/

# sops leftover dumps
*.bak
secrets/**/*.bak

# Ansible: vault password is local-only (not sops). Retry files and
# BERT/MCA dumps are recreated by ansible-playbook / one-off host analysis.
ansible/.vault_pass.txt
ansible/*.retry
ansible/.ansible/
ansible/*.bin
